Location-Based Mall Navigator App

## To run the app 
- Clone the repo to your device. Download Expo Go app.
- Smartphone and the device you are running code on must be within same network. Use wifi, avoid VPN.

1. Install dependencies.
```bash
npm install
```
2. Start the app.
```bash
npm start
or
npx expo start -c //To clear the cache
```

constant > appConstants public mode radius
privatemode > constant > privatemodeConstants private mode radius